spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/zhishiyun?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root

kb:
  jwt:
    secret: dev-secret-dev-secret-dev-secret
  internal:
    api-key: internal-dev-key
  rag:
    hybrid-enabled: true
  vision:
    enabled: true
  sso:
    mock-enabled: true
  rate-limit:
    enabled: true
    ask-per-minute: 10
