# MVP 阶段技术选型建议

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | MVP 阶段技术选型建议 |
| 对应阶段 | 第一阶段：MVP 核心能力（预计 6 周） |
| 文档版本 | v1.0 |
| 创建日期 | 2026-07-28 |

---

## 1. 选型原则

MVP 阶段的核心目标是 **快速打通 RAG 全链路，验证核心产品价值**。技术选型遵循以下原则：

| 原则 | 说明 |
|------|------|
| **快速验证** | 优先选择成熟、开箱即用的技术，减少自研成本 |
| **生态完善** | 选择社区活跃、文档完善、组件丰富的技术栈 |
| **可扩展性** | 技术架构需预留扩展空间，便于后续阶段平滑升级 |
| **国产兼容** | 优先支持私有化部署和国内可用组件，降低合规风险 |
| **成本可控** | 在满足需求的前提下，优先选择开源免费方案 |

---

## 2. MVP 阶段业务范围

| 功能模块 | MVP 交付内容 |
|----------|--------------|
| 知识库管理 | 基础知识库创建、文档上传（PDF/Word/Excel） |
| 文档解析 | 原生 PDF、Word、Excel 基础解析 |
| 向量化存储 | 文本切片、Embedding 生成、Milvus 存储 |
| 智能问答 | 单轮问答、向量检索 + BM25 混合检索、基础答案溯源 |
| 系统管理 | 用户登录、基础角色权限、操作日志 |

---

## 3. 总体技术架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                         前端层                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  React 18 + Ant Design Pro  /  Vue 3 + Element Plus          │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         API 网关层                                   │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │  Spring Cloud Gateway / Nginx                                │  │
│  │  路由转发 / 统一鉴权 / 限流熔断 / 日志记录                    │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         后端服务层                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐     │
│  │ 知识库服务   │  │ 文档解析服务 │  │  智能问答服务       │     │
│  │ (Spring Boot)│  │ (Python      │  │  (Spring Boot +     │     │
│  │              │  │  FastAPI)    │  │   Python Worker)    │     │
│  └──────────────┘  └──────────────┘  └──────────────────────┘     │
│  ┌──────────────┐  ┌──────────────┐                                │
│  │ 用户权限服务 │  │ 操作日志服务 │                                │
│  │ (Spring Boot)│  │ (Spring Boot)│                                │
│  └──────────────┘  └──────────────┘                                │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         数据存储层                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐     │
│  │ PostgreSQL   │  │ Milvus       │  │  MinIO / 本地存储   │     │
│  │ 业务数据     │  │ 向量数据     │  │  文件对象存储       │     │
│  └──────────────┘  └──────────────┘  └──────────────────────┘     │
│  ┌──────────────┐  ┌──────────────┐                                │
│  │ Redis        │  │ Elasticsearch│                                │
│  │ 缓存/会话    │  │ BM25 全文索引│                                │
│  └──────────────┘  └──────────────┘                                │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         AI 能力层                                    │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐     │
│  │ 大语言模型   │  │ Embedding   │  │  文本向量检索       │     │
│  │ 通义千问     │  │ BGE /       │  │  (Milvus HNSW)      │     │
│  │ Qwen2.5      │  │ m3e-large   │  │                     │     │
│  └──────────────┘  └──────────────┘  └──────────────────────┘     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 4. 后端技术栈选型

### 4.1 主服务框架

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **首选** | Spring Boot 3.x + Java 17/21 | 企业级生态成熟，适合业务服务（知识库、用户权限、日志等） |
| **备选** | Python FastAPI | 适合 AI 相关服务快速开发，可与 Java 服务协同 |

**建议**：MVP 阶段采用 **Java（Spring Boot）+ Python（FastAPI）混合架构**：
- Java 负责：业务服务、用户权限、操作日志、知识库管理
- Python 负责：文档解析、Embedding 生成、AI 模型调用

### 4.2 微服务与通信

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **服务通信** | OpenFeign / WebClient | Spring Boot 服务间调用 |
| **异步任务** | RabbitMQ / Redis Streams | 文档解析、Embedding 生成等异步处理 |
| **任务调度** | XXL-Job / PowerJob | 定时任务、批量任务调度（MVP 可先用 Spring Scheduler） |

### 4.3 ORM 与数据访问

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **ORM 框架** | MyBatis-Plus | 国内使用广泛，代码生成器完善，适合快速开发 |
| **数据库连接池** | HikariCP | Spring Boot 默认，性能优秀 |
| **缓存框架** | Spring Cache + Redis | 缓存用户会话、热点知识库元数据 |

---

## 5. 前端技术栈选型

### 5.1 前端框架

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **首选方案 A** | React 18 + Ant Design Pro | 企业级中后台方案成熟，组件丰富 |
| **首选方案 B** | Vue 3 + Element Plus | 国内开发者熟悉，学习成本较低 |

**MVP 推荐**：如果团队 React 经验丰富，选 **Ant Design Pro**；如果追求快速上手，选 **Vue 3 + Element Plus**。

### 5.2 状态管理与网络请求

| 选型 | React 方案 | Vue 方案 |
|------|-----------|----------|
| 状态管理 | Zustand / Redux Toolkit | Pinia |
| 网络请求 | Axios + React Query | Axios + Vue Request |
| 路由 | React Router v6 | Vue Router 4 |
| 构建工具 | Vite | Vite |

### 5.3 文件上传组件

| 选型 | 说明 |
|------|------|
| Ant Design Upload / Element Upload | 基础上传组件 |
| react-dropzone / vue-dropzone | 拖拽上传增强 |
| 分片上传 | 自行封装或集成 tus-js-client |

---

## 6. 数据存储选型

### 6.1 关系型数据库

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **首选** | PostgreSQL 15+ | 支持 JSONB、全文检索，扩展性好，适合知识库元数据和文档元数据 |
| **备选** | MySQL 8.0 | 国内团队更熟悉，生态成熟 |

**MVP 推荐 PostgreSQL**：
- 原生支持 JSONB，便于存储文档解析结果、操作日志内容等半结构化数据
- 全文检索能力可为后续 BM25 关键词检索提供补充

### 6.2 向量数据库

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **首选** | Milvus 2.3+ | 企业级向量数据库，支持分布式、HNSW 索引、与 LangChain 集成完善 |
| **备选** | Qdrant / Weaviate | 轻量级，适合小规模部署 |
| **备选** | pgvector | 如果数据量小，可直接用 PostgreSQL 插件，降低运维成本 |

**MVP 推荐**：优先使用 **Milvus Standalone 模式**，后续可平滑升级到集群模式。

### 6.3 全文检索

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **首选** | Elasticsearch 8.x | BM25 算法成熟，与 RAG 混合检索集成方便 |
| **备选** | PostgreSQL 全文检索 | 数据量小时可减少组件，降低复杂度 |

**MVP 推荐**：如果团队熟悉 ES，直接上 Elasticsearch；如果追求极简，可先用 PostgreSQL 全文检索，第二阶段再引入 ES。

### 6.4 缓存与会话

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **缓存** | Redis 7.x | 用户会话、热点数据缓存、分布式锁 |
| **会话存储** | Redis | 存储登录态、多轮对话上下文（MVP 阶段对话较简单） |

### 6.5 对象存储

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **首选** | MinIO | 开源对象存储，S3 兼容，适合私有化部署 |
| **备选** | 阿里云 OSS / 腾讯云 COS | 公有云部署时选用 |
| **备选** | 本地文件系统 | MVP 极简方案，但不利于扩展 |

---

## 7. 文档解析技术选型

### 7.1 文档解析方案对比

| 文档类型 | 推荐方案 | 说明 |
|----------|----------|------|
| **PDF** | PyMuPDF (fitz) + pdfplumber | PyMuPDF 速度快，pdfplumber 表格提取效果好 |
| **Word** | python-docx | 标准 DOCX 解析，结构保留较好 |
| **Excel** | openpyxl / pandas | 读取单元格文本和公式结果 |
| **通用** | Apache Tika | 通用文档解析，可作为兜底方案 |

### 7.2 解析服务架构

建议将文档解析作为独立 Python 服务，通过消息队列与主服务解耦：

```
文档上传
    │
    ▼
┌─────────────────┐
│  知识库服务     │  保存文件元数据，发送解析任务
│  (Spring Boot)  │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   RabbitMQ      │  异步任务队列
└────────┬────────┘
         │
         ▼
┌─────────────────────────┐
│   文档解析服务          │  解析文档，提取文本和元数据
│   (Python + FastAPI)    │
└────────┬────────────────┘
         │
         ▼
┌─────────────────────────┐
│   向量化服务            │  文本切片、Embedding 生成
│   (Python + FastAPI)    │
└────────┬────────────────┘
         │
         ▼
       Milvus / PostgreSQL
```

### 7.3 文本切片方案

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **切片框架** | LangChain / LlamaIndex | 提供成熟的文本分割器 |
| **中文优化** | 自定义 RecursiveCharacterTextSplitter | 按段落、句号、换行分层切分 |
| **Token 计算** | tiktoken / jieba + 自定义规则 | 英文用 tiktoken，中文可结合 jieba |

**MVP 默认切片参数**：
- chunk_size: 800 tokens
- chunk_overlap: 100 tokens
- 优先按语义边界切分

---

## 8. 向量检索与 RAG 技术选型

### 8.1 Embedding 模型

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **中文首选** | BGE-M3 / bge-large-zh-v1.5 | 北京智源开源，中文语义理解优秀 |
| **通用多语言** | m3e-large / multilingual-e5-large | 支持多语言，适合混合语种场景 |
| **部署方式** | Xinference / Ollama / vLLM | 本地私有化部署 Embedding 服务 |
| **调用方式** | HuggingFace Transformers | 直接加载模型推理 |

**MVP 推荐**：
- 模型：**bge-large-zh-v1.5**（中文场景）或 **BGE-M3**（支持多语言和稀疏向量）
- 部署：**Xinference** 或 **Ollama**，提供统一模型服务接口

### 8.2 大语言模型

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **国产首选** | 通义千问 Qwen2.5 / Qwen-7B/14B | 开源可私有化部署，中文能力强 |
| **备选国产** | 智谱 GLM-4 / ChatGLM3-6B | 中文对话效果优秀 |
| **备选国产** | 百度文心一言 / 讯飞星火 | API 调用，无需本地部署 |
| **海外** | GPT-4 / Claude 3 | 能力强但需考虑合规和网络 |

**MVP 推荐**：
- 如果允许调用 API：**通义千问 API** 或 **文心一言 API**
- 如果需要私有化：**Qwen2.5-7B-Instruct** 或 **Qwen2.5-14B-Instruct**，配合 vLLM 推理加速

### 8.3 RAG 框架

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **首选** | LangChain | 生态最完善，组件丰富，适合快速搭建 RAG |
| **备选** | LlamaIndex | 检索和索引能力强，适合复杂知识库场景 |
| **自研** | 自行封装向量检索 + Prompt 模板 | 灵活可控，但开发成本较高 |

**MVP 推荐**：使用 **LangChain** 快速搭建 RAG 链路，后续根据需求逐步替换为自研。

### 8.4 混合检索实现

| 检索路径 | 技术实现 |
|----------|----------|
| 向量检索 | Milvus / pgvector + Embedding 模型 |
| 关键词检索 | Elasticsearch BM25 / PostgreSQL 全文检索 |
| 结果融合 | RRF (Reciprocal Rank Fusion) 或 加权求和 |

**RRF 融合公式**：
```
score = Σ 1 / (k + rank_i)
```
其中 k 通常取 60，rank_i 为各检索路径中的排序位置。

---

## 9. 安全与认证选型

### 9.1 身份认证

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **JWT 认证** | Spring Security + JWT | MVP 阶段快速实现登录态 |
| **密码加密** | BCrypt | 用户密码安全存储 |
| **会话管理** | Redis + JWT | Token 过期管理，支持登出 |

### 9.2 权限控制

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **RBAC** | Spring Security + 自定义注解 | 基于角色的访问控制 |
| **数据权限** | MyBatis-Plus 数据权限插件 / 自定义 AOP | 按知识库、部门过滤数据 |

### 9.3 传输与存储安全

| 能力 | 推荐方案 |
|------|----------|
| 传输加密 | HTTPS / TLS 1.2+ |
| 敏感字段加密 | AES-256-GCM |
| 文件存储加密 | MinIO 服务端加密或磁盘加密 |
| SQL 注入防护 | MyBatis 参数化查询 |

---

## 10. 部署与 DevOps 选型

### 10.1 容器化与编排

| 选型 | 推荐方案 | 说明 |
|------|----------|------|
| **容器化** | Docker + Docker Compose | MVP 阶段快速部署所有依赖 |
| **编排（可选）** | Kubernetes | 如果团队已有 K8s 经验，可直接使用 |

### 10.2 开发工具链

| 环节 | 推荐方案 |
|------|----------|
| 代码仓库 | GitLab / GitHub |
| CI/CD | GitLab CI / GitHub Actions |
| 接口文档 | Knife4j / SpringDoc OpenAPI |
| 日志收集 | ELK Stack（Elasticsearch + Logstash + Kibana）|
| 监控告警 | Prometheus + Grafana |
| 链路追踪 | SkyWalking / Zipkin（可选）|

### 10.3 MVP 推荐部署拓扑

```
┌─────────────────────────────────────────────────────┐
│                    单服务器 / 虚拟机                 │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │ Nginx       │  │ Spring Boot │  │ Python      │ │
│  │ (网关)      │  │ (主服务)    │  │ (AI 服务)   │ │
│  └─────────────┘  └─────────────┘  └─────────────┘ │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐ │
│  │ PostgreSQL  │  │ Milvus      │  │ Redis       │ │
│  │ (业务数据)  │  │ (向量数据)  │  │ (缓存)      │ │
│  └─────────────┘  └─────────────┘  └─────────────┘ │
│  ┌─────────────┐  ┌─────────────┐                   │
│  │ MinIO       │  │ RabbitMQ    │                   │
│  │ (文件存储)  │  │ (消息队列)  │                   │
│  └─────────────┘  └─────────────┘                   │
└─────────────────────────────────────────────────────┘
```

---

## 11. 技术选型总览表

| 模块 | 推荐方案 | 备选方案 | 选型理由 |
|------|----------|----------|----------|
| 后端主框架 | Spring Boot 3.x + Java 17 | Node.js NestJS | 企业级成熟、生态完善 |
| AI 服务框架 | Python FastAPI | Flask | 高性能异步、AI 生态好 |
| 前端框架 | React 18 + Ant Design Pro | Vue 3 + Element Plus | 企业级中后台成熟 |
| 关系型数据库 | PostgreSQL 15+ | MySQL 8.0 | JSONB、全文检索能力强 |
| 向量数据库 | Milvus 2.3+ Standalone | pgvector / Qdrant | 企业级、可扩展 |
| 全文检索 | Elasticsearch 8.x | PostgreSQL 全文检索 | BM25 成熟、与 RAG 集成方便 |
| 缓存 | Redis 7.x | - | 会话、缓存、分布式锁 |
| 对象存储 | MinIO | 本地文件系统 | S3 兼容、私有化部署 |
| 消息队列 | RabbitMQ | Redis Streams | 文档解析异步解耦 |
| PDF 解析 | PyMuPDF + pdfplumber | Apache Tika | 速度与表格质量兼顾 |
| Word 解析 | python-docx | Apache Tika | 结构保留好 |
| Excel 解析 | openpyxl + pandas | - | 公式结果提取方便 |
| Embedding 模型 | bge-large-zh-v1.5 | BGE-M3 / m3e-large | 中文语义优秀 |
| 大语言模型 | 通义千问 Qwen2.5 | GLM-4 / 文心一言 | 中文能力强、可私有化 |
| RAG 框架 | LangChain | LlamaIndex | 生态最完善 |
| 模型推理部署 | Xinference / vLLM | Ollama | 统一模型服务、高性能 |
| 身份认证 | Spring Security + JWT | OAuth2.0（第四阶段） | MVP 快速实现 |
| 权限控制 | RBAC + 自定义数据权限 | - | 满足企业隔离需求 |
| 容器化 | Docker Compose | Kubernetes | MVP 快速部署 |
| 监控 | Prometheus + Grafana | - | 开源成熟 |
| 日志 | ELK Stack | Loki + Grafana | 日志集中分析 |

---

## 12. 推荐最小可行技术栈组合

针对 MVP 阶段（6 周内交付），推荐以下最小可行组合：

```
后端：Spring Boot 3.x + Java 17 + MyBatis-Plus + PostgreSQL 15
AI 服务：Python 3.11 + FastAPI + LangChain
前端：React 18 + Ant Design Pro + Vite
向量库：Milvus 2.3 Standalone
缓存：Redis 7
文件存储：MinIO
消息队列：RabbitMQ
模型：bge-large-zh-v1.5（Embedding）+ 通义千问 Qwen2.5（LLM）
部署：Docker Compose
监控：Prometheus + Grafana
```

---

## 13. 各阶段升级路径

| 当前阶段 | 下一阶段升级重点 |
|----------|------------------|
| **MVP 阶段** | 基础 RAG 链路、单轮问答、3 种文档格式 |
| 第二阶段 | 引入 OCR（PaddleOCR / Tesseract）、PPT 解析、图片视觉理解模型 |
| 第三阶段 | 引入 Rerank 模型（bge-reranker）、意图识别模型、Query Rewrite |
| 第四阶段 | SSO 对接（OAuth2.0 / SAML）、企业级监控、API 开放平台 |

---

## 14. 注意事项

1. **Embedding 模型选型**：MVP 阶段优先选择中文模型，如果企业文档包含大量英文或混合语种，建议评估 BGE-M3 的多语言能力。
2. **大模型部署方式**：如果私有化部署资源有限，可先调用云端 API（如通义千问 API），后续再切换为本地模型。
3. **Milvus 部署模式**：MVP 使用 Standalone 模式即可，数据量增长后再考虑 Cluster 模式。
4. **文档解析准确性**：MVP 阶段扫描件 OCR 不是重点，可先用开源方案兜底，第二阶段重点优化。
5. **成本控制**：MVP 阶段所有组件均可选择开源免费方案，降低试错成本。

---

## 15. 附录

### 附录 A：推荐开源组件版本

| 组件 | 推荐版本 |
|------|----------|
| Spring Boot | 3.2.x |
| Java | 17 LTS 或 21 LTS |
| Python | 3.11 |
| FastAPI | 0.110+ |
| LangChain | 0.2.x |
| PostgreSQL | 15+ |
| Milvus | 2.3+ |
| Redis | 7.x |
| Elasticsearch | 8.x |
| MinIO | latest stable |
| RabbitMQ | 3.12+ |
| React | 18.x |
| Node.js | 18 LTS |

### 附录 B：MVP 阶段技术栈决策树

```
是否需要私有化部署大模型？
    ├── 是 → 选择 Qwen2.5-7B/14B + vLLM/Xinference
    └── 否 → 选择通义千问 API / 文心一言 API

团队前端技术栈？
    ├── React 熟练 → Ant Design Pro
    └── Vue 熟练 → Element Plus

数据量预期？
    ├── < 100 万条向量 → Milvus Standalone
    └── > 100 万条向量 → Milvus Cluster（后续升级）

是否需要 BM25 关键词检索？
    ├── 是 → Elasticsearch
    └── 否 → PostgreSQL 全文检索（MVP 可简化）
```

### 附录 C：版本历史

| 版本 | 日期 | 修改内容 | 作者 |
|------|------|----------|------|
| v1.0 | 2026-07-28 | 初始版本 | AI Assistant |

---

*文档结束*
